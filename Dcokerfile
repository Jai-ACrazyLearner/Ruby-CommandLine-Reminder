FROM ruby:2.1-onbuild

ENV APP_HOME /doctorSuchitraApps
RUN mkdir $APP_HOME
WORKDIR $APP_HOME
COPY ./ $APP_HOME

RUN echo "alias remindme='rake timerApp:reminde_me'" >> ~/.bashrc

RUN echo "alias repeat timer='rake timerApp:repeatTimer'" >> ~/.bashrc